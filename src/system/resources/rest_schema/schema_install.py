install_attributes = {
    'service': {
        'type': str,
        'required': True
    },
    'version': {
        'type': str,
        'required': True
    }
}

install_plugin_attributes = {
    'plugin': {
        'type': str,
        'required': True
    }
}
